
distribution=_DISTRIBUTION
package_manager=_PACKAGE_MANAGER
kernel=_KERNEL
arch=_ARCH

#if defined pacman
PACMAN=true
#else
PACMAN=false
#endif

#if defined pacmang2
PACMAN_G2=true
#else
PACMAN_G2=false
#endif

#if defined dpkg
DPKG=true
#else
DPKG=false
#endif

#if defined rpm
RPM=true
#else
RPM=false
#endif

#if defined pkgtools
PKGTOOLS=true
#else
PKGTOOLS=false
#endif

#if defined nhopkg
NHOPKG=true
#else
NHOPKG=false
#endif

#if defined opkg
OPKG=true
#else
OPKG=false
#endif

#if defined crux
Crux=true
#else
Crux=false
#endif

#if defined debian
Debian=true
#else
Debian=false
#endif

#if defined arch
Arch=true
#else
Arch=false
#endif

#if defined frugalware
Frugalware=true
#else
Frugalware=false
#endif

#if defined freebsd
FreeBSD=true
#else
FreeBSD=false
#endif

#if defined mandriva
Mandriva=true
#else
Mandriva=false
#endif

#if defined arch
Arch=true
#else
Arch=false
#endif

#if defined slackware
Slackware=true
#else
Slackware=false
#endif
