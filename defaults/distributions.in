
distribution=@DISTRIBUTION@
package_manager=@PACKAGE_MANAGER@
kernel=@KERNEL@
arch=@ARCH@

#if defined pacmang2
FPM=true
#else
FPM=false
#endif

#if defined dpkg
DPKG=true
#else
DPKG=false
#endif

#if defined rpm
RPM=true
#else
RPM=false
#endif

#if defined pkgtools
PKGTOOLS=true
#else
PKGTOOLS=false
#endif

#if defined crux
Crux=true
#else
Crux=false
#endif

#if defined debian
Debian=true
#else
Debian=false
#endif

#if defined frugalware
Frugalware=true
#else
Frugalware=false
#endif

#if defined freebsd
FreeBSD=true
#else
FreeBSD=false
#endif

#if defined mandriva
Mandriva=true
#else
Mandriva=false
#endif

#if defined arch
Arch=true
#else
Arch=false
#endif

#if defined slackware
Slackware=true
#else
Slackware=false
#endif
