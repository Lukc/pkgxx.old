
// vim: syntax=sh

: ${sysconfdir:="/etc"}
: ${localstatedir:="/var"}
: ${prefix:="/usr"}
: ${eprefix:="$prefix"}
: ${bindir:="$eprefix/bin"}
: ${libdir:="$prefix/lib"}
: ${libexecdir:="$eprefix/libexec"}
: ${sharedir:="$prefix/share"}
: ${datadir:="$prefix/share"}
: ${docdir:="$data/doc/$name-$version"}
#if defined crux || defined slackware
: ${mandir:="$prefix/man"}
#else
: ${mandir:="$sharedir/man"}
#endif

