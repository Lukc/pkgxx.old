#
## Include for themes packages
#
# Need $name, $version, $release and $source
# Can receive arguments from $themes
#

build() {
	mkdir -p $PKG/usr/share/themes/
	cp -r ${themes[@]} $PKG$sharedir/themes/
}

# vim: syntax=sh shiftwidth=4 tabstop=4
