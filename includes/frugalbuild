
# 
# Include used by modules/frugalbuild. It allows using FrugalBuilds instead 
#+ of Pkgfiles. This is a very slow method, but it works less or more. (= with
#+ some bugs.)
# 
# Tested and known to work on simple packages, on simple situations.
# 

export name=$pkgname
export version=$pkgver
export release=$pkgrel
export description=$pkgdesc

unset archs # Bug with pkg++ archs… will be removed latter.

export Fdestdir="$PKGMK_WORK_DIR/pkg"
export Fsrcdir="$PKGMK_WORK_DIR/src"
export startdir=$PKGMK_WORK_DIR

function msg {
	echo -e "\033[00;34m   > \033[00m$@"
}

function Fmessage {
	echo -e "\033[00;34m   > \033[00m$@"
}

